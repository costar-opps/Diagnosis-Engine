## 1. 骨架与样例数据

- [x] 1.1 在 `index.html` 中重排 `#aiopsMonitorView`：新增工具栏、`#dePlatformPanel`、`#deProjectPanel`、`#deReportPanel` 四个容器
- [x] 1.2 将既有 `#aiopsMonitorDashboard` 节点移入 `#dePlatformPanel`，保持 id 与 class 不变
- [x] 1.3 将既有 `#aiopsMonitorReport` 节点移入 `#deReportPanel`，保持 id 与 class 不变
- [x] 1.4 工具栏新增项目切换器 `#deProjectSelect`、刷新按钮 `#deRefreshBtn`、诊断形态标识 `#deModeBadge`
- [x] 1.5 侧栏新增项目列表容器 `#deProjectList`
- [x] 1.6 各分区新增标题与归属说明，平台层标注为全平台共享，项目层显示当前项目名
- [x] 1.7 新增 `diagnosis-sample-data.js`，导出样例项目列表与样例项目概览常量，不含逻辑
- [x] 1.8 在 `index.html` 中引入 `diagnosis-sample-data.js`，置于 `aiops-monitor.js` 之前
- [x] 1.9 `styles.css` 新增 `de-` 前缀样式：工具栏、分区容器、项目卡片、指标项、异常项、报告操作条
- [x] 1.10 `styles.css` 新增样例与占位角标样式、禁用态样式
- [x] 1.11 验证既有「查看当前监控数据」路径仍可正常渲染平台层

## 2. 项目切换

- [x] 2.1 在应用状态中新增 `currentProjectId` 字段，并定义其 localStorage 独立键
- [x] 2.2 实现 `loadProjects()`：请求项目列表，失败时回落到样例常量并置分区为样例态
- [x] 2.3 实现 `renderProjectList(projects)`：渲染侧栏列表，展示名称、可诊断状态与异常标记
- [x] 2.4 实现项目列表空状态渲染
- [x] 2.5 实现 `renderProjectSelect(projects)`：填充顶部切换器选项
- [x] 2.6 实现 `setCurrentProject(projectId)`：更新状态、同步两处入口的选中态、持久化
- [x] 2.7 实现启动时的项目恢复：读取持久化值，不在列表中则回退首个可诊断项目并提示
- [x] 2.8 切换项目时立即清空项目层并置为加载态，不保留上一项目数据
- [x] 2.9 切换项目时刷新报告层为该项目的最近报告或空状态
- [x] 2.10 不可诊断项目在列表与切换器中显示状态与原因，并禁用诊断入口
- [x] 2.11 对项目层请求做去抖，并在响应返回时校验其项目标识仍为当前选中项

## 3. 平台层与刷新编排

- [x] 3.1 实现 `renderPlatformPanel(dashboard)`：内部调用既有 `renderDashboardIntoContainer`，不重写渲染逻辑
- [x] 3.2 实现平台层加载失败态：显示失败提示与重试入口，不显示零值
- [x] 3.3 实现 `refreshConsole()`：以 `Promise.allSettled` 并行发起平台层与项目层请求
- [x] 3.4 平台层与项目层各自处理失败，互不影响对方渲染
- [x] 3.5 绑定工具栏刷新按钮到 `refreshConsole()`
- [x] 3.6 `switchMainView('aiops-monitor')` 进入控制台时触发一次 `refreshConsole()`
- [x] 3.7 验证切换项目时平台层内容保持不变

## 4. 项目层视图

- [x] 4.1 实现 `fetchProjectSummary(projectId)`：请求项目概览，失败回落样例并置样例态
- [x] 4.2 实现 `renderProjectPanel(summary)`：以数据驱动方式渲染指标列表，不写死卡片数量
- [x] 4.3 指标项渲染名称、当前值、单位与业务含义
- [x] 4.4 指标超阈值时应用异常样式
- [x] 4.5 指标 `available` 为否时渲染为不可用，不显示零值
- [x] 4.6 渲染活跃异常列表：名称、严重级别、持续时长
- [x] 4.7 渲染无活跃异常时的正常状态
- [x] 4.8 渲染最近变更信息，并区分「不可用」与「近期无变更」两种状态
- [x] 4.9 新增一键诊断按钮 `#deRunDiagnosisBtn`，携带当前项目标识与项目级形态
- [x] 4.10 项目不可诊断或诊断进行中时禁用诊断按钮
- [x] 4.11 实现诊断进行中的界面状态提示
- [x] 4.12 实现诊断失败的错误展示并恢复按钮可用
- [x] 4.13 未选中项目时项目层显示选择项目的引导

## 5. 后端占位接口

- [x] 5.1 新建 `DiagnosisConsoleController`，映射到 `/api/diagnosis`
- [x] 5.2 定义项目列表项与项目概览的响应模型，字段命名与设计中的契约一致
- [x] 5.3 指标项包含 `available` 字段，变更信息包含 `available` 字段
- [x] 5.4 实现 `GET /projects`：返回静态项目列表，响应带 `placeholder` 标记
- [x] 5.5 实现 `GET /projects/{id}/summary`：已知标识返回静态概览，未知标识返回 404
- [x] 5.6 实现 `POST /diagnoses`：返回 501 与明确的未实现说明，不返回伪造报告
- [x] 5.7 实现 `POST /reports/{id}/feedback`：接收并回显，带占位标记，不持久化
- [x] 5.8 确认未修改 `MonitoringController` 与 `AiOpsDashboardService`
- [x] 5.9 前端改为优先请求占位接口，失败时才降级到样例数据

## 6. 数据可信度与降级

- [x] 6.1 定义分区级数据状态：真实、占位、样例
- [x] 6.2 占位响应的 `placeholder` 标记驱动分区显示占位角标
- [x] 6.3 接口失败驱动分区显示样例角标
- [x] 6.4 样例态下禁用采纳、驳回等提交类入口
- [x] 6.5 样例态下诊断入口给出说明提示
- [x] 6.6 验证降级按分区独立生效，项目层降级时平台层不显示样例角标
- [x] 6.7 未注册项目的 404 响应在界面上展示为项目不存在提示

## 7. 报告层与反馈

- [x] 7.1 将报告的 localStorage 结构由单键改为按 `projectId` 分槽，平台级报告使用固定槽位
- [x] 7.2 实现旧结构探测与迁移：识别为旧结构时迁移到平台级槽位并保留原数据
- [x] 7.3 报告层展示报告内容、诊断形态、所属项目与生成时间
- [x] 7.4 无报告时展示空状态与发起诊断引导
- [x] 7.5 新增追踪标识展示区，支持复制
- [x] 7.6 新增可折叠的工具调用摘要区域
- [x] 7.7 追踪信息缺失时显示为暂不可用，不展示虚构内容
- [x] 7.8 新增采纳与驳回操作入口
- [x] 7.9 驳回时要求填写原因，为空则阻止提交
- [x] 7.10 反馈提交后展示结果与时间，并禁用重复提交
- [x] 7.11 反馈提交失败时提示失败并保留已填写内容供重试

## 8. 文案与收尾

- [x] 8.1 页面标题、侧栏、工具栏文案统一为 `diagnosis-engine · 诊断控制台`
- [x] 8.2 移除界面中指代同一功能的「智能 OnCall 助手」「AI Ops」旧称谓
- [x] 8.3 诊断相关文案区分「平台级诊断」与「项目级诊断」
- [x] 8.4 更新静态资源版本号参数，避免浏览器缓存旧脚本与样式
- [x] 8.5 回归验证对话视图：发送消息、流式输出、文件上传、历史会话均不受影响
- [x] 8.6 回归验证既有 AI Ops 分析路径仍可产出报告并展示在报告层
- [x] 8.7 端到端验证：切换项目、平台层不变、项目层刷新、样例角标、反馈交互
- [x] 8.8 更新 `docs/启动手册.md`，补充控制台使用方式与占位接口说明
