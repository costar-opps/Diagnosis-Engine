## 1. 项目档案与注册表

- [x] 1.1 定义 `ProjectProfile` 数据结构，覆盖 identity / datasources / knowledge / business / dependencies / ops 六个分组
- [x] 1.2 实现 YAML 档案加载器，从配置目录读取 `config/projects/<projectId>.yml`
- [x] 1.3 实现档案校验器：标识唯一、必填齐全、日志源类型受支持、指标表达式非空、依赖引用存在
- [x] 1.4 实现注册表：加载失败的项目标记为不可诊断并保留原因，不影响其他项目
- [x] 1.5 实现显式重载接口，重载失败时保持原有注册表不变
- [x] 1.6 提供项目列表与单项目档案摘要的查询接口
- [x] 1.7 编写主角项目 `card-service` 档案，与现有本机数据源等价，用于验证链路
- [x] 1.8 为档案校验编写单元测试，覆盖标识重复、依赖悬空、必填缺失三类场景

## 2. 工具层项目路由

- [x] 2.1 定义 `DiagnosisContext`，携带权威 `projectId`、诊断形态、统一时间窗
- [x] 2.2 `QueryMetricsTools` 增加 `projectId` 参数，按档案指标定义解析表达式
- [x] 2.3 `QueryMetricsTools` 区分平台共享指标与项目业务指标两类查询入口
- [x] 2.4 未声明指标返回明确的未定义提示，禁止用近似指标替代
- [x] 2.5 `QueryLogsTools` 增加 `projectId` 参数，按档案日志源定位符路由
- [x] 2.6 实现日志字段映射与归一化，输出统一结构，缺失字段标记为不可用
- [x] 2.7 实现工具层权威校验：模型传入的 `projectId` 与上下文不一致或未注册时直接拒绝
- [x] 2.8 新增变更事件查询工具，区分「无变更」与「变更源不可达」
- [x] 2.9 实现代码线索定位：按 `packagePrefix` 筛选堆栈帧，映射到文件与方法并关联原始日志
- [x] 2.10 统一查询失败的返回结构，携带失败数据源与原因，供置信度计算使用
- [x] 2.11 为工具路由编写测试：未注册项目拒绝、不可诊断项目拒绝、字段映射归一

## 3. 诊断形态与编排

- [ ] 3.1 定义诊断请求模型，包含形态与可选项目标识，校验形态与标识的组合合法性
- [ ] 3.2 实现 `DiagnosisContext` 装配：加载档案、归一时间基准、计算时间窗
- [ ] 3.3 将 Planner 与 Executor 提示词改造为模板 + 变量注入，移除硬编码业务描述
- [ ] 3.4 实现平台级提示词变量装配：受影响项目摘要、共享资源指标清单
- [ ] 3.5 实现项目级提示词变量装配：关键链路、已知故障模式、可用业务指标
- [ ] 3.6 实现按形态裁剪工具可见集，平台级不暴露项目日志工具
- [ ] 3.7 实现告警驱动的形态分流：按告警归属判定，归属模糊时先执行平台资源扫描
- [ ] 3.8 实现平台扫描结果作为上下文传递，避免运行中形态切换
- [ ] 3.9 实现跨项目一跳扩展：由下游依赖类证据触发，依赖未声明时不扩展并说明原因
- [ ] 3.10 实现扩展留痕，将扩展范围写入诊断上下文供报告引用
- [ ] 3.11 增加单次诊断的总时长与工具调用次数上限，并限制并发诊断数量
- [ ] 3.12 为形态分流编写测试：主机告警走平台级、带项目标签告警走项目级、多项目异常两种分支

## 4. 上下文与记忆工程（取证驾驭层）

- [x] 4.1 定义记忆分层模型：会话短期记忆、单次诊断轨迹、长期知识，明确各层可见边界
- [x] 4.2 定义证据黑板结构：形态、项目/受影响项目、告警、证据、假设、失败工具、预算与裁剪记录
- [x] 4.3 实现每轮上下文装配：只注入黑板摘要与本步工具摘要，不回放已被覆盖的原始正文
- [x] 4.4 实现会话短期记忆的项目标识关联，会话内切换项目后清空业务证据区且不沿用前一项目语义
- [x] 4.5 实现诊断轨迹生命周期：当次结束即封存，默认不跨次复用
- [x] 4.6 实现形态上下文防火墙：平台级限制单项目业务日志的证据角色，项目级绑定当前档案
- [x] 4.7 实现工具返回的摘要化拦截层，超阈值时先在服务端摘要再进入上下文
- [x] 4.8 实现日志聚合摘要：按错误签名分类，输出出现次数、时间分布、代表性样本、省略规模
- [x] 4.9 实现指标返回的时间窗降采样摘要
- [x] 4.10 摘要携带 `rawRef`（时间范围、总条数、查询标识），支持溯源到原始记录
- [x] 4.11 实现报告校验：无 `rawRef` 的内容不得进入根因，只能进入待确认线索
- [x] 4.12 实现单次诊断 Token 预算估算与上限配置
- [x] 4.13 实现超限裁剪：保留证据要点与最近推理步骤，丢弃原始正文与已被摘要覆盖的中间结果
- [x] 4.14 实现裁剪留痕，写入黑板并在报告的诊断过程中体现
- [x] 4.15 实现预算耗尽时的显式终止：输出部分结论并标注原因；裁剪或预算终止时置信度不得为高
- [x] 4.16 实现记忆晋升约束：未采纳轨迹不回流；已发布知识命中标注 `historical_clue`，仍须本次取证
- [x] 4.17 为上下文机制编写测试：摘要阈值、`rawRef` 校验、裁剪优先级、预算耗尽降置信度、切换项目不串用、轨迹不跨次

## 5. 知识隔离

- [ ] 5.1 Milvus 集合 schema 增加 `project_id` 与 `scope` 标量字段
- [ ] 5.2 文档写入链路强制要求归属元数据，缺失时拒绝写入
- [ ] 5.3 编写历史文档迁移脚本，按目录批量补写归属，无法判定的标为 `platform`
- [ ] 5.4 迁移期保留旧集合只读，检索层增加切回旧集合的开关
- [ ] 5.5 `InternalDocsTools` 按诊断形态构造检索过滤条件
- [ ] 5.6 实现项目级双取：当前项目专属文档 + 平台通用文档
- [ ] 5.7 实现平台级检索仅覆盖平台通用文档
- [ ] 5.8 实现跨项目检索开关，默认关闭，结果标注来源项目与「仅供参考」性质
- [ ] 5.9 检索结果携带归属范围与来源标识，供报告溯源引用
- [ ] 5.10 实现无命中时的明确反馈，禁止虚构文档来源
- [ ] 5.11 整理平台通用文档与项目专属文档各若干份，验证隔离效果

## 6. 知识自进化闭环

- [ ] 6.1 定义候选知识结构：故障特征、证据模式、根因结论、处置步骤、归属、来源报告标识
- [ ] 6.2 实现采纳触发的候选知识生成，驳回不生成候选
- [ ] 6.3 低置信度报告生成的候选标注来源置信度，供确认环节参考
- [ ] 6.4 实现候选暂存区，未确认的候选不进入检索范围
- [ ] 6.5 实现人工确认与发布流程，支持发布前修改候选内容
- [ ] 6.6 实现候选拒绝并记录原因
- [ ] 6.7 实现相似度检测与去重：超阈值走更新路径，保留历史版本标识与更新原因
- [ ] 6.8 沉淀文档写入时携带归属、来源报告标识与沉淀时间
- [ ] 6.9 实现引用沉淀知识时的溯源标注，报告中说明来自哪次历史诊断
- [ ] 6.10 实现驳回记录的查询与原因汇总
- [ ] 6.11 实现驳回记录与故障场景的关联，作为场景判定的补充依据
- [ ] 6.12 为自进化闭环编写测试：采纳生成候选、未确认不可检索、相似候选走更新

## 7. 诊断报告

- [ ] 7.1 定义报告数据模型：标识、生成时间、形态、概览、时间线、证据链、根因、置信度、建议、工具摘要
- [ ] 7.2 实现多源事件时间对齐，保留各事件原始精度标记
- [ ] 7.3 实现证据链分类组织：指标、日志、变更、代码线索
- [ ] 7.4 实现论断与证据的引用关联，无证据论断归入「待确认线索」
- [ ] 7.5 实现服务端置信度上限计算：依据关键数据源成败、知识命中、代码线索有无、候选原因数量
- [ ] 7.6 将上下文裁剪与预算终止纳入置信度上限的判定因子
- [ ] 7.7 实现处置建议来源标注，区分文档出处、沉淀知识与模型建议
- [ ] 7.8 编写平台级与项目级两套报告模板骨架，共用证据链与置信度规则
- [ ] 7.9 接入内嵌数据库，持久化报告并保证生成后不可变
- [ ] 7.10 实现采纳与驳回反馈接口，记录结果、原因与时间
- [ ] 7.11 实现报告更新走「新报告 + 关联旧报告」路径
- [ ] 7.12 为置信度降级规则编写测试：日志失败降级、多候选原因降级、预算终止降级

## 8. Agent 执行可观测

- [x] 8.1 实现诊断追踪标识的生成与全链路透传，中断的诊断同样保留标识
- [x] 8.2 定义执行轨迹事件模型，覆盖节点流转、LLM 调用、工具调用三类
- [x] 8.3 在 Graph 节点的进入与退出处埋点，记录顺序与耗时
- [x] 8.4 在 LLM 调用处埋点，记录模型标识、输入输出 Token、耗时、成败、重试次数
- [x] 8.5 实现提示词与响应正文留存的开关，关闭时保留全部结构化字段
- [x] 8.6 在工具调用处埋点，记录工具名、参数、成败、耗时、返回摘要
- [x] 8.7 记录被服务端校验拒绝的工具调用及拒绝原因
- [x] 8.8 实现轨迹落库与按追踪标识的回放查询，可重建完整执行序列
- [x] 8.9 实现轨迹异步写入，写入失败不阻塞诊断主流程并记录失败
- [x] 8.10 通过 Micrometer 暴露运行指标：诊断次数与成败、耗时、工具调用次数、Token 消耗、工具失败率
- [x] 8.11 指标按诊断形态与项目维度打标签，避免使用高基数字段作为标签
- [x] 8.12 报告的工具调用摘要改为读取轨迹数据，避免重复记录
- [x] 8.13 为可观测编写测试：轨迹可重建、写入失败不中断诊断、指标维度正确

## 9. 故障场景与评测基线

- [x] 9.1 搭建下游项目 `payment-service`，具备独立日志与业务指标
- [x] 9.2 搭建配角项目 `order-service`，极简实现，仅产生日志与少量指标
- [x] 9.3 为演示项目实现故障注入端点，支持注入与撤销
- [x] 9.4 编写主机资源压力脚本，覆盖内存与磁盘两类场景并设定安全阈值
- [x] 9.5 定义场景描述结构：标识、类别、注入方式、预期形态、期望证据、结论要点、通过下限
- [x] 9.6 落地平台类场景：主机内存打满、磁盘将满
- [x] 9.7 落地项目类场景：接口超时、关键业务成功率骤降、变更后错误尖刺、无告警静默故障
- [x] 9.8 落地关联类场景：下游依赖故障、同机资源争用
- [x] 9.9 实现场景注入的可撤销校验，确认撤销后指标与日志恢复
- [x] 9.10 实现结论要点的逐条判定逻辑，输出明确的通过或未通过
- [x] 9.11 实现场景执行记录的持久化与历史对比查询
- [x] 9.12 将场景库拆分为演示集与保留集，保留集不参与提示词调优
- [x] 9.13 实现跑批入口，汇总通过率、平均工具调用次数与 Token 消耗
- [ ] 9.14 完成一轮全场景基线跑批，记录初始通过率与成本基线

## 10. 前端与配置

- [ ] 10.1 `application.yml` 新增档案目录、默认诊断形态、跨项目扩展与跨项目检索开关
- [ ] 10.2 新增上下文配置项：摘要阈值、Token 预算上限、裁剪策略参数
- [ ] 10.3 新增可观测配置项：轨迹留存开关、提示词正文留存开关、轨迹保留期
- [ ] 10.4 保留 mock 双模开关，语义调整为全局数据源模拟开关
- [ ] 10.5 `AiOpsDashboardService` 拆分为平台层与项目层两部分数据
- [ ] 10.6 前端增加顶部项目切换器，切换后刷新项目层视图并保留平台层常驻
- [ ] 10.7 前端实现平台概览视图：共享资源、项目健康摘要、异常项目计数
- [ ] 10.8 前端实现项目详情视图：业务指标、活跃异常、最近变更、一键诊断入口
- [ ] 10.9 前端实现报告页：时间线、证据链分类展开、置信度标识、建议来源标注
- [ ] 10.10 前端实现采纳与驳回交互，驳回支持填写原因
- [ ] 10.11 前端实现候选知识的确认、修改与发布入口
- [ ] 10.12 前端实现执行轨迹的折叠展示：工具调用序列、Token 消耗与追踪标识

## 11. 收尾验证

- [ ] 11.1 全模拟环境下复跑全部场景，确认可复现
- [ ] 11.2 真实环境下完成一次端到端演示，覆盖平台级与项目级各一个场景
- [ ] 11.3 验证回滚路径：注册表退回单项目模式、检索层切回旧集合、可观测埋点停用
- [ ] 11.4 验证边界约束：跨项目串味、越界查询、二跳扩展、跨会话记忆串用均被正确拒绝
- [ ] 11.5 验证成本控制：长链路场景下上下文裁剪与预算终止按预期生效
- [ ] 11.6 验证自进化闭环：采纳后候选可确认发布，发布后能被后续诊断检索命中
- [x] 11.7 更新 `README.md` 与启动手册，说明项目注册、场景注入、两种诊断形态与知识沉淀的使用方式
